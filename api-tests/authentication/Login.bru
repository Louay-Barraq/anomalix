meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
         "username": "N00006",
         "password": "pass0006"
  }
}

script:post-response {
  const data = res.getBody();
  if (data.token) {
      bru.setEnvVar("token", data.token);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
